# PM LESSONS LEARNED — НЕ ПОВТОРЯТЬ!
## FYPFixer Project

---

## ❌ ОШИБКА 1: SQLite в тестах для PostgreSQL проекта

**Дата:** 18 Dec 2025

**Что сделал неправильно:**
- Написал integration tests, которые используют `create_app('testing')`
- Не проверил что `TestingConfig` использует `sqlite:///:memory:`
- Модели используют PostgreSQL-специфичные типы (JSONB, BigInteger)
- Результат: 17 тестов skipped

**Правильное решение:**
- Тесты должны использовать ту же БД что и prod (PostgreSQL)
- `TestingConfig` → `postgresql://...fypfixer_test`
- Создать отдельную тестовую БД

**Урок:**
```
ПЕРЕД написанием тестов — ПРОВЕРЬ TestingConfig!
```

---

## ❌ ОШИБКА 2: JSONB вместо JSON

**Дата:** 18 Dec 2025

**Что сделал неправильно:**
- В моделях использовал `JSONB` (PostgreSQL-only)
- Это работает в prod, но ломает тесты на SQLite

**Правильное решение:**
- Использовать универсальный `JSON` тип
- Или сразу настроить тесты на PostgreSQL

**Урок:**
```
Если проект использует PostgreSQL — тесты тоже должны использовать PostgreSQL!
Не пытаться адаптировать под SQLite.
```

---

## ❌ ОШИБКА 3: Слишком большие промпты

**Дата:** 18 Dec 2025

**Что сделал неправильно:**
- Пытался создать один огромный промпт для Onboarding Flow
- Система зависла

**Правильное решение:**
- Разбивать на маленькие шаги (5-10 мин каждый)
- Один промпт = одна задача

**Урок:**
```
Максимум 1 файл или 1 фича на промпт!
Лучше 10 маленьких промптов чем 1 большой.
```

---

## ❌ ОШИБКА 4: Frontend вызывает старый API

**Дата:** 18 Dec 2025 (найдено при аудите)

**Что было:**
- Backend: создан новый AI Pipeline `/api/v1/plans/today`
- Frontend: всё ещё вызывал старый `/api/actions`
- AI Pipeline не использовался!

**Урок:**
```
После создания нового API — СРАЗУ проверить что Frontend его использует!
Аудит Frontend-Backend интеграции обязателен.
```

---

## ✅ ПРАВИЛА НА БУДУЩЕЕ

1. **Тесты = та же БД что и prod**
   - PostgreSQL проект → PostgreSQL тесты
   - Не адаптировать под SQLite

2. **Маленькие промпты**
   - 1 промпт = 1 задача = 5-10 мин
   - Не объединять несколько фич

3. **Проверять интеграцию**
   - Новый API → сразу обновить Frontend
   - Аудит после каждой большой фичи

4. **Проверять конфиги**
   - Перед тестами — посмотреть TestingConfig
   - Перед деплоем — посмотреть ProductionConfig

---

## 📋 ЧЕКЛИСТ ПЕРЕД НАПИСАНИЕМ ТЕСТОВ

- [ ] Какую БД использует TestingConfig?
- [ ] Совпадает ли с production БД?
- [ ] Есть ли PostgreSQL-специфичные типы в моделях?
- [ ] Создана ли тестовая БД?

---

*Обновлять этот файл при каждой новой ошибке!*
