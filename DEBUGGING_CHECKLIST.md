# DEBUGGING_CHECKLIST.md — Критические правила отладки

**Created:** 17 Dec 2025, 22:30 MSK  
**Reason:** Day 4 — потеряно 2 часа на debugging из-за невнимательности  
**Lesson:** ROUTING FIRST, TypeScript second

---

## 🚨 ROUTING FIRST RULE

### Проблема: "Показывает старую страницу / старый UI"

**❌ НЕ НАЧИНАЙ С:**
- TypeScript errors
- API debugging
- Docker rebuild
- Cache clearing

**✅ НАЧНИ С:**
1. **Проверь router.tsx/router.ts** — какой компонент используется?
2. **Проверь импорты** — нет ли V0/Old/Deprecated версий?
3. **Grep по проекту** — ищи "V0", "old", "deprecated", "legacy"

### Пример из Day 4:

```typescript
// ❌ ПРОБЛЕМА была здесь:
import { DashboardPageV0 } from './pages/dashboard/DashboardPageV0';

// ✅ ДОЛЖНО БЫТЬ:
import { DashboardPage } from './pages/dashboard/DashboardPage';
```

**Результат:** 2 часа потеряно на исправление TypeScript ошибок, хотя проблема была в routing!

---

## 📋 ОБЯЗАТЕЛЬНЫЙ ЧЕК-ЛИСТ ПЕРЕД РЕШЕНИЕМ

Когда анализируешь проблему с UI/Frontend:

```markdown
☐ 1. Проверил router.tsx — какие компоненты рендерятся?
☐ 2. Проверил entry points (App.tsx, main.tsx) — правильные импорты?
☐ 3. Grep по "V0", "old", "deprecated" — нет ли legacy кода?
☐ 4. Проверил что frontend билдится БЕЗ ошибок
☐ 5. Проверил что API endpoint существует и работает
☐ 6. Проверил браузер DevTools Console — какие ошибки?
☐ 7. Проверил Network tab — какие запросы идут?
```

**ТОЛЬКО ПОСЛЕ ВСЕХ ПУНКТОВ — давать решение!**

---

## 🎯 ПРАВИЛА ДЛЯ PM (Claude Sonet)

### При анализе репозитория:

1. **ВСЕГДА проверяй routing ПЕРВЫМ**
   - `router.tsx`, `App.tsx`, `main.tsx`
   - Какие компоненты импортируются?
   - Нет ли старых версий (V0, Old, etc)?

2. **GREP по legacy keywords**
   ```bash
   grep -r "V0\|old\|deprecated\|legacy" Frontend/src/
   ```

3. **Проверяй BUILD перед инструкциями**
   - Код может быть правильным
   - Но роутинг может указывать на старую версию

---

## 🔧 ТИПИЧНЫЕ ОШИБКИ

### Ошибка #1: TypeScript First (Day 4)

**Что было:**
- Увидел TypeScript ошибки в логах
- Начал чинить типы 2 часа
- Проблема была в router.tsx (DashboardPageV0)

**Что должно быть:**
- Проверил router.tsx FIRST
- Увидел бы проблему за 30 секунд

### Ошибка #2: Не проверил entry points

**Симптом:** "Код обновлён, но UI старый"

**Причины:**
- Router использует старый компонент
- App.tsx импортирует старую версию
- Два файла с похожими именами (Page vs PageV0)

**Решение:**
- Проверяй ВСЕ импорты в routing файлах

---

## 📝 КОМАНДЫ ДЛЯ БЫСТРОЙ ПРОВЕРКИ

### 1. Найти все V0/old компоненты:
```bash
find Frontend/src -name "*V0*" -o -name "*old*" -o -name "*deprecated*"
```

### 2. Проверить router импорты:
```bash
grep -n "import.*Dashboard" Frontend/src/router.tsx
grep -n "import.*Dashboard" Frontend/src/App.tsx
```

### 3. Проверить что компонент используется:
```bash
grep -r "DashboardPage" Frontend/src/router.tsx Frontend/src/App.tsx
```

---

## 🚀 ПРОЦЕСС DEBUGGING (ПРАВИЛЬНЫЙ)

### Шаг 1: Определи симптом
- UI показывает старую версию?
- TypeScript не компилируется?
- API не отвечает?

### Шаг 2: Routing First (если UI проблема)
```bash
# Проверь router
cat Frontend/src/router.tsx | grep -A 5 "path.*dashboard"

# Проверь импорты
head -20 Frontend/src/router.tsx
```

### Шаг 3: Entry Points
```bash
# Проверь App.tsx
cat Frontend/src/App.tsx | grep "import"
```

### Шаг 4: Legacy код
```bash
# Найди все старые версии
find Frontend/src -name "*V0*"
grep -r "deprecated\|legacy" Frontend/src/
```

### Шаг 5: Browser DevTools
- Console: ошибки?
- Network: какие запросы?
- Elements: какой компонент рендерится?

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**Для Founder:**
- Если я (PM) не показал тебе router.tsx при анализе UI проблемы — ОСТАНОВИ МЕНЯ
- Требуй: "Покажи router.tsx ПЕРВЫМ!"

**Для PM (Claude Sonet):**
- UI проблема = ROUTING FIRST
- Не давай решение без проверки routing
- Чек-лист обязателен ПЕРЕД решением

---

## 📚 LESSONS LEARNED

### Day 4 (17 Dec 2025):
- **Проблема:** Dashboard показывает старый UI
- **Потеряно:** 2 часа
- **Причина:** router.tsx использовал DashboardPageV0
- **Решение:** Claude Code нашёл за 30 секунд
- **Урок:** ROUTING FIRST — ВСЕГДА!

---

## ✅ SUCCESS CRITERIA

Этот чек-лист успешен если:
- Больше НЕ теряем время на ложные проблемы
- Routing проверяется ПЕРВЫМ при UI issues
- Чек-лист выполняется ПЕРЕД решением
- Founder видит router.tsx в анализе

---

**ЗАПОМНИ: UI проблема = router.tsx FIRST, всё остальное — потом!** 🎯
