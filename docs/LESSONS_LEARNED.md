# FYPGlow — Lessons Learned

> Ошибки, которые мы совершили, и правила чтобы их не повторять.
> **ОБЯЗАТЕЛЬНО К ПРОЧТЕНИЮ** перед деплоем!

**Последнее обновление:** 24 декабря 2025

---

## 🎂 День рождения FYPGlow: 24 декабря 2025

TikTok LIVE одобрен! 🎉

---

## 🏛️ Базовые принципы

1. **Секреты никогда в Git** — только .env.example
2. **Код должен быть полным** — каждый import имеет файл
3. **Одна правда** — без дублирования
4. **Environment = Конфигурация** — один код, разные .env
5. **Проверяй перед деплоем** — тест → push → deploy → logs

---

## 🚨 Ошибки

### #1: Placeholder вместо реальных ключей
Никогда не писать `PUT_YOUR_KEY_HERE`

### #2: TikTok PKCE — HEX vs Base64URL
Desktop: HEX (64 символа), Web: Base64URL (~43 символа)

### #3: Один redirect_uri для двух сред
Всегда два URI в TikTok Portal

### #4: .env файлы в Git
.gitignore ОБЯЗАТЕЛЬНО содержит все .env

### #5: Отсутствующий файл при import
Каждый import должен иметь реальный файл

### #6: Дублирующаяся директория
Удалять deprecated код сразу

### #7: Docker не читает .env при build
Использовать `--build-arg` при сборке

### #8: Case-sensitive пути в Linux
`./frontend` ≠ `./Frontend` на Linux!

### #9: Дефолтный VITE_API_URL = localhost
Всегда передавать --build-arg на prod

---

## 🔧 Полезные команды

### Деплой frontend
```bash
docker compose build \
  --build-arg VITE_TIKTOK_CLIENT_KEY=awvyb17mzijy09je \
  --build-arg VITE_API_URL=https://fypglow.com/api \
  --no-cache frontend && docker compose up -d frontend
```

### Проверка ключей
```bash
docker compose exec backend env | grep TIKTOK
docker compose exec frontend sh -c "cat /usr/share/nginx/html/assets/*.js | grep -o 'awvyb17mzijy09je'"
```
